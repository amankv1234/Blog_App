<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top shadow-sm">
  <div class="container">
    <!-- Brand -->
    <a class="navbar-brand fw-bold fs-4 text-white" href="/">Blogify</a>

    <!-- Mobile Toggle -->
    <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
      aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Navbar Links -->
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0 align-items-lg-center">
        <!-- Home -->
        <li class="nav-item">
          <a class="nav-link text-white px-3" href="/">Home</a>
        </li>

        <!-- If user logged in -->
        <% if (locals.user) { %>
          <!-- Add Blog -->
          <li class="nav-item">
            <a class="nav-link text-white px-3" href="/blog/add-new">Add Blog</a>
          </li>

          <!-- User Profile Dropdown -->
          <li class="nav-item dropdown">
           <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
  <img src="<%= user.profileImageURL || '/images/default-avatar.png' %>" alt="profile" class="rounded-circle me-2" width="30" height="30">
  <%= user.fullName %>
</a>
            <ul class="dropdown-menu dropdown-menu-end rounded-3 shadow">
              <li><a class="dropdown-item" href="/user/logout">Logout</a></li>
            </ul>
          </li>

        <% } else { %>
          <!-- Signup / Signin -->
          <li class="nav-item">
            <a class="nav-link text-white px-3" href="/user/Signup">Create Account</a>
          </li>
          <li class="nav-item">
            <a class="nav-link text-white px-3" href="/user/Signin">Signin</a>
          </li>
        <% } %>

        
      </ul>
    </div>
  </div>
</nav>

<!-- Error Alert -->
<% if (locals.error) { %>
  <div class="container mt-5 pt-5">
    <div class="alert alert-danger mt-4 text-center rounded-pill shadow-sm" role="alert">
      <%= locals.error %>
    </div>
  </div>
<% } %>

<!-- ===== CSS Styling ===== -->
<style>
  /* ======= Black Navbar Style ======= */
  nav.navbar {
    background-color: #000 !important;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }

  .navbar-brand {
    color: #fff !important;
    letter-spacing: 1px;
    transition: color 0.3s ease;
  }

  .navbar-brand:hover {
    color: #ffd43b !important;
  }

  .navbar-nav .nav-link {
    transition: all 0.3s ease;
    font-weight: 500;
  }

  .navbar-nav .nav-link:hover {
    color: #ffd43b !important;
    transform: translateY(-2px);
  }

  .btn-outline-light {
    transition: all 0.3s ease;
  }

  .btn-outline-light:hover {
    background-color: #ffd43b;
    color: #000;
    border-color: #ffd43b;
  }

  /* User Avatar */
  .navbar .rounded-circle {
    object-fit: cover;
  }

  /* Prevent overlap with fixed navbar */
  body {
    padding-top: 80px;
  }

  /* Mobile Navbar */
  @media (max-width: 992px) {
    .navbar-collapse {
      background-color: #000;
      border-radius: 10px;
      padding: 10px;
    }
    .navbar-nav .nav-link {
      color: #fff !important;
    }
  }
</style>
